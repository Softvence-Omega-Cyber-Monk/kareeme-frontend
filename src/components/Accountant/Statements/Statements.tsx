import StatementComponent from "./StatementComponent";

const Statements = () => {
  return (
    <div className="space-y-6">
      <StatementComponent />
    </div>
  );
};

export default Statements;
